# ha-kiosk-google-calendar-sync.service already creates/owns this via its
# own RuntimeDirectory=kiosk. This is only needed when the daemon is run
# outside that unit (manual `--once` testing, a different supervisor).
d /run/kiosk 0750 kiosk kiosk -
